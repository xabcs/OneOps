# 监控中心文档

## 一、Agent 指标收集体系

### 1.1 Agent 架构

OneOps Agent 是部署在目标主机上的轻量级监控代理，负责采集系统指标并上报给 OneOps 后端。

### 1.2 监听端口

- **默认端口**：`9100`
- **配置方式**：启动参数 `-port <端口号>`

### 1.3 指标端点

| 端点路径 | 方法 | 说明 |
|---------|------|------|
| `/metrics` | GET | 基础系统指标 |
| `/extended-metrics` | GET | 扩展指标数据 |
| `/health` | GET | 健康检查接口 |

### 1.4 基础指标字段

```json
{
  "cpuUsage": 23.5,          // CPU使用率 (%)
  "memoryUsage": 68.2,       // 内存使用率 (%)
  "diskUsage": 45.8,         // 磁盘使用率 (%)
  "diskPartitions": [...],   // 磁盘分区详情
  "load5": 1.2,              // 5分钟负载
  "processNum": 156,         // 进程数量
  "version": "1.0.0"         // Agent版本号
}
```

### 1.5 数据采集频率

| 指标类型 | 采集间隔 |
|---------|---------|
| 基础指标 | 60秒 |
| 扩展指标 | 120秒 |
| 心跳上报 | 60秒 |

---

## 二、指标采集机制

### 2.1 CPU 采样策略

Agent 采用增量采样方式：

- 启动时先执行一次"热身"调用建立基准值
- 等待 3 秒后正式采样
- `cpu.Percent(0, false)` 获取上次调用以来的增量值
- 避免首次读数恒为 0 的问题

### 2.2 磁盘分区过滤

自动过滤以下临时文件系统：

- `tmpfs`
- `devtmpfs`
- `overlay`
- `squashfs`
- `shm`

### 2.3 缓存机制

指标数据采用读写锁保护的缓存结构：

- `metricsCache`：基础指标缓存
- `extMetricsCache`：扩展指标缓存
- 后台采样线程更新缓存
- HTTP 请求直接读取缓存，无阻塞

---

## 三、心跳机制

### 3.1 心跳配置

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `-heartbeat-url` | 后端心跳接收地址 | 空（不发送） |
| `-heartbeat-token` | 心跳鉴权 Token | 空 |

### 3.2 心跳Payload结构

```json
{
  "hostname": "server-01",
  "ip": "192.168.1.100",
  "pid": 12345,
  "port": 9100,
  "version": "1.0.0",
  "token": "xxx"
}
```

### 3.3 心跳流程

```
Agent启动 → 立即发送首次心跳 → 每60秒定时发送 → 后端记录在线状态
```

---

## 四、后端集成

### 4.1 指标获取接口

后端通过 HTTP 请求获取 Agent 指标：

```
GET http://<agent-ip>:9100/metrics
```

### 4.2 心跳接收端点

后端提供心跳接收接口：

```
POST /api/cmdb/agent/heartbeat
```

### 4.3 指标同步流程

```
Agent采集 → 缓存更新 → 后端定时拉取 → 数据库存储 → 前端展示
```

---

## 五、启动命令示例

```bash
# 最简启动（仅本地采集，不上报）
./oneops-agent

# 指定端口
./oneops-agent -port 9101

# 配置心跳上报
./oneops-agent -heartbeat-url http://backend:8082/api/cmdb/agent/heartbeat -heartbeat-token xxx
```

---

## 六、常见问题

### 6.1 指标为 0 的问题

**原因**：首次调用 `cpu.Percent(0, false)` 时返回 0

**解决**：Agent 已内置热身机制，启动后等待 3 秒再首次采样

### 6.2 磁盘指标异常

**检查项**：

- 确认磁盘分区挂载正常
- 检查是否被过滤（tmpfs 等）
- 确认 Agent 有读取权限

### 6.3 心跳失败

**检查项**：

- 后端服务是否正常运行
- 网络连通性（防火墙、端口开放）
- 心跳 URL 配置是否正确
