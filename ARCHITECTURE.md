# OneOps K8s诊断功能完整架构

## 🎯 系统概述

OneOps K8s诊断功能是一个完整的Java应用诊断解决方案，集成了Web界面、后端服务和K8s Agent，用户可以通过友好的Web界面对K8s集群中的Java应用进行实时诊断。

## 🏗️ 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        OneOps 前端                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ K8s集群管理  │  │   诊断中心   │  │   Pod管理    │     │
│  │   页面       │  │   页面       │  │   页面       │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                          ↓ HTTP API
┌─────────────────────────────────────────────────────────────┐
│                      OneOps 后端服务                         │
│  ┌──────────────────┐  ┌──────────────────┐              │
│  │ 诊断控制器        │  │ 诊断服务层        │              │
│  │ (Controller)     │  │ (Service)        │              │
│  └──────────────────┘  └──────────────────┘              │
│           ↓                        ↓                        │
│  ┌──────────────────┐  ┌──────────────────┐              │
│  │ K8s服务层       │  │ 数据库服务       │              │
│  │ (K8s Service)   │  │ (DB Service)     │              │
│  └──────────────────┘  └──────────────────┘              │
└─────────────────────────────────────────────────────────────┘
                          ↓ K8s API
┌─────────────────────────────────────────────────────────────┐
│                      Kubernetes 集群                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                DaemonSet Agent                        │   │
│  │  ┌────────────┐      ┌────────────┐                 │   │
│  │  │ Node 1     │      │ Node 2     │                 │   │
│  │  │ Agent      │      │ Agent      │                 │   │
│  │  └────────────┘      └────────────┘                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  Sidecar Agent                       │   │
│  │  ┌─────────────────────────────────────┐            │   │
│  │  │ Java Pod                           │            │   │
│  │  │  ┌──────────────┐  ┌──────────────┤            │   │
│  │  │  │ Java App     │  │ Arthas Agent │            │   │
│  │  │  │ Container    │  │ Container    │            │   │
│  │  │  └──────────────┘  └──────────────┘            │   │
│  │  └─────────────────────────────────────┘            │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 核心组件

### 1. 前端组件

#### 诊断页面 (`/k8s/diagnostic`)
- **功能**：主要的诊断操作界面
- **特性**：
  - 集群和命名空间选择
  - Java应用Pod列表展示
  - 诊断命令选择器
  - 参数配置界面
  - 实时结果展示
  - 历史记录查看

#### Pod管理页面 (`/k8s/pods`)
- **功能**：Pod管理界面集成诊断入口
- **特性**：
  - Pod列表显示诊断状态
  - 快速诊断按钮
  - 诊断结果快速查看

### 2. 后端组件

#### 诊断控制器 (`DiagnosticController`)
- **功能**：处理诊断相关的HTTP请求
- **接口**：
  - `GET /api/v1/diagnostic/commands` - 获取诊断命令
  - `GET /api/v1/diagnostic/pods/:clusterId/:namespace` - 获取可诊断Pods
  - `POST /api/v1/diagnostic/execute/:clusterId/:namespace/:podName` - 执行诊断
  - `GET /api/v1/diagnostic/history/:clusterId/:namespace/:podName` - 获取历史记录

#### 诊断服务 (`DiagnosticService`)
- **功能**：核心诊断逻辑实现
- **方法**：
  - `getJavaPods()` - 获取Java应用Pod列表
  - `checkAgentStatus()` - 检查Agent状态
  - `executeViaSidecar()` - Sidecar方式诊断
  - `executeViaDaemonSet()` - DaemonSet方式诊断
  - `saveDiagnosticHistory()` - 保存诊断历史

#### K8s服务 (`KubernetesService`)
- **功能**：K8s API交互
- **方法**：
  - `getPods()` - 获取Pod列表
  - `execInPod()` - Pod中执行命令
  - `getNamespaces()` - 获取命名空间

### 3. Agent组件

#### DaemonSet Agent
- **部署**：每个节点一个Agent Pod
- **功能**：诊断节点上所有Java应用
- **通信**：HTTP API (8888端口)
- **优势**：资源共享，部署简单

#### Sidecar Agent
- **部署**：每个Java Pod一个Agent容器
- **功能**：诊断同Pod内的Java应用
- **通信**：HTTP API (8563端口)
- **优势**：性能最优，真正attach

## 🔄 工作流程

### 用户操作流程

```
1. 用户访问诊断页面
   ↓
2. 选择集群和命名空间
   ↓
3. 系统获取Java应用Pod列表
   ↓
4. 用户选择目标Pod
   ↓
5. 用户选择诊断命令
   ↓
6. 配置命令参数
   ↓
7. 点击执行诊断
   ↓
8. 系统执行诊断并显示结果
   ↓
9. 用户查看、复制或下载结果
   ↓
10. 系统保存诊断历史
```

### 系统执行流程

```
1. 前端发送诊断请求
   ↓
2. 后端接收请求，验证权限
   ↓
3. 检查Pod的Agent状态
   ↓
4. 根据Agent类型选择执行方式
   ↓
5a. Sidecar方式：直接调用Pod内Agent
   ↓
5b. DaemonSet方式：通过节点Agent执行
   ↓
6. Agent执行Arthas诊断命令
   ↓
7. 收集诊断结果
   ↓
8. 返回结果给前端
   ↓
9. 前端展示结果
   ↓
10. 保存诊断历史到数据库
```

## 📊 数据流

### 请求数据流

```
前端 → HTTP POST → 后端控制器 → 诊断服务 → K8s服务 → Agent → Java应用
```

### 响应数据流

```
Java应用 → Agent → K8s服务 → 诊断服务 → 后端控制器 → 前端展示
```

## 🛡️ 安全机制

### 1. 认证授权
- 用户登录认证
- 权限验证：`k8s:diagnostic:execute`
- API访问控制

### 2. 输入验证
- 命令白名单验证
- 参数安全检查
- Pod名称格式验证

### 3. 网络安全
- 集群内通信
- HTTPS加密传输
- NetworkPolicy隔离

### 4. 审计日志
- 诊断操作记录
- 用户行为追踪
- 错误日志保存

## 📈 性能优化

### 1. 前端优化
- 虚拟滚动：大量Pod列表
- 结果分页：诊断结果展示
- 缓存机制：减少API调用

### 2. 后端优化
- 连接池：K8s API连接复用
- 结果缓存：相同命令结果缓存
- 异步处理：长时间诊断异步执行

### 3. Agent优化
- 资源限制：CPU和内存控制
- 连接复用：Arthas连接保持
- 超时控制：防止资源耗尽

## 🎯 使用场景

### 1. 性能问题诊断
- CPU使用率异常高
- 内存泄漏分析
- 线程死锁检测
- 响应时间过长

### 2. 应用监控
- 实时应用状态
- JVM信息查看
- 内存使用分析
- 线程状态监控

### 3. 问题排查
- 应用异常分析
- 日志级别调整
- 类加载问题
- 内存泄漏检测

## 🔧 运维管理

### 1. Agent管理
```bash
# 查看DaemonSet Agent状态
kubectl get pods -l app=diagnostic-agent -n kube-system

# 查看Sidecar Agent状态
kubectl get pods -n production -o wide

# 查看Agent日志
kubectl logs <agent-pod> -n <namespace>
```

### 2. 监控指标
- 诊断执行次数
- 诊断成功率
- 平均响应时间
- Agent健康状态

### 3. 告警规则
- Agent不可用告警
- 诊断失败率过高
- 响应时间过长
- 资源使用异常

## 🎓 最佳实践

### 1. 部署策略
- **开发环境**：使用DaemonSet方式
- **生产环境**：关键应用使用Sidecar
- **混合部署**：根据应用重要性选择

### 2. 权限管理
- 最小权限原则
- 按角色分配权限
- 定期权限审计

### 3. 监控告警
- 设置合理的监控指标
- 配置告警规则
- 定期检查系统状态

### 4. 性能优化
- 根据负载调整资源限制
- 优化网络配置
- 定期清理历史数据

## 🚀 未来规划

### 短期目标
- 支持更多诊断命令
- 实现WebSocket实时流
- 添加结果分析功能

### 中期目标
- 机器学习异常检测
- 自动化诊断建议
- 性能趋势分析

### 长期目标
- 全栈性能监控
- 智能故障诊断
- 预测性维护

这个完整的架构设计为OneOps平台提供了强大的K8s Java应用诊断能力，极大提升了运维效率和问题排查能力。