# 主机资产管理页面布局说明

## 页面路径
`/Users/mpm/doc/go/OneOpsV2/OneOps/soybean-admin-element-plus/src/views/cmdb_servers/index.vue`

## 整体布局结构

页面采用左右分栏布局，使用 Flexbox 实现：

```
┌─────────────────────────────────────────────────────────────────┐
│                     h-full flex gap-12px                        │
├──────────────────┬────────────────────────────────────────────┤
│   左侧分组树     │              右侧主机列表                     │
│   width: 220px  │             flex-1                          │
│                  │                                              │
│  ┌────────────┐ │  ┌──────────────────────────────────────┐  │
│  │ 资产分组    │ │  │ 操作栏 (Proxmox风格)                  │  │
│  │ [+] [刷新]  │ │  │ ┌──────────┬────────────────────┐   │  │
│  │ [搜索框]    │ │  │ │创建|更多 │ [筛选▼] [搜索] [🔄] │   │  │
│  │            │ │  │ └──────────┴────────────────────┘   │  │
│  │ 分组树     │ │  └──────────────────────────────────────┘  │
│  │            │ │                                              │
│  │ 根节点     │ │  ┌──────────────────────────────────────┐  │
│  │  ├─分组1   │ │  │ 数据表格 (ElTable)                    │  │
│  │  ├─分组2   │ │  │ - 复选框列                            │  │
│  │  └─分组3   │ │  │ - 主机名 (可点击)                     │  │
│  │            │ │  │ - IP地址                              │  │
│  └────────────┘ │  │ - 配置                                │  │
│                  │  │ - 环境                                │  │
│                  │  │ - 分组                                │  │
│                  │  │ - 资源使用率                          │  │
│                  │  │ - CPU趋势                             │  │
│                  │  │ - 服务状态                            │  │
│                  │  │ - 告警                                │  │
│                  │  │ - 操作                                │  │
│                  │  └──────────────────────────────────────┘  │
│                  │                                              │
│                  │  ┌──────────────────────────────────────┐  │
│                  │  │ 分页组件                               │  │
│                  │  └──────────────────────────────────────┘  │
└──────────────────┴────────────────────────────────────────────┘
```

## 详细布局说明

### 1. 外层容器

```vue
<div class="h-full flex gap-12px overflow-hidden">
```

- `h-full`: 高度100%，占满父容器
- `flex`: 使用Flexbox布局
- `gap-12px`: 左右分栏间距12px
- `overflow-hidden`: 隐藏溢出内容

### 2. 左侧分组树区域

#### 容器定义

```vue
<div class="w-220px flex flex-col flex-shrink-0">
```

- `w-220px`: 固定宽度220px
- `flex flex-col`: 垂直方向Flex布局
- `flex-shrink-0`: 不会收缩

#### 内容结构

```
┌──────────────────────────┐
│ ElCard (shadow="never")  │
├──────────────────────────┤
│ 树头部                   │
│ ├─ 标题 "资产分组"        │
│ └─ 操作按钮 [+] [刷新]    │
├──────────────────────────┤
│ 搜索框                   │
│ [🔍] [搜索分组...]       │
├──────────────────────────┤
│ 树内容区域 (flex-1)       │
│ └─ ElTree 组件           │
│     ├─ 根节点 "资产树"    │
│     └─ 各级分组节点       │
└──────────────────────────┘
```

**样式特点：**
- Card使用 `shadow="never"` 无阴影
- `body-style="padding: 12px"` 内边距12px
- 树内容区域 `flex-1 overflow-auto` 自适应高度并可滚动

#### 树节点特性

1. **右键菜单功能**
   - 重命名分组
   - 添加子分组
   - 删除分组
   - 添加主机到该分组

2. **内联编辑**
   - 点击重命名后，节点名称变为输入框
   - 支持Enter保存、Escape取消
   - 失焦自动保存

3. **图标显示**
   - 每个分组有自定义颜色和图标
   - 图标使用 `component` 动态渲染
   - 格式: `icon-{icon-name}`

### 3. 右侧主机列表区域

#### 容器定义

```vue
<div class="min-w-0 flex flex-col flex-1">
  <div class="flex-1 flex flex-col bg-white overflow-hidden">
```

- `min-w-0`: 最小宽度为0，允许Flex子项收缩
- `flex-1`: 占据剩余空间
- `bg-white`: 白色背景

#### 操作栏 (Proxmox风格)

```vue
<div class="p-12px border-b border-gray-200" style="background-color: #f5f7fa">
```

**布局结构：**

```
┌───────────────────────────────────────────────────────────┐
│ flex items-center justify-between gap-12px               │
├────────────────────────────┬──────────────────────────────┤
│ 左侧操作区                  │ 右侧操作区                     │
│                            │                              │
│ ┌─────────────────────┐   │ ┌──────────────────────────┐ │
│ │ [创建 ▼] [更多操作▼]│   │ │ [筛选▼] [搜索...] [🔄]   │ │
│ └─────────────────────┘   │ └──────────────────────────┘ │
└────────────────────────────┴──────────────────────────────┘
```

**左侧按钮：**
- **创建按钮** (绿色 #00A67D)
  - 下拉菜单: 新增主机、批量导入
- **更多操作** 按钮
  - 仅当选中主机时可用
  - 下拉菜单: 测试连接、批量部署、批量卸载、批量删除

**右侧组件：**
- **筛选下拉** (120px宽)
  - 选项: 主机名、IP地址、分组
- **搜索输入框** (200px宽)
  - 支持Enter键搜索
  - 带搜索图标后缀
- **刷新按钮** (text样式)
  - 加载时显示旋转动画

#### 数据表格

```vue
<ElTable
  height="100%"
  size="small"
  border
  stripe
  table-layout="fixed"
  :row-style="{ height: '48px' }"
  :cell-style="{ padding: '0' }"
  :header-cell-style="{ backgroundColor: '#f5f7fa' }"
>
```

**表格特性：**
- 固定表头，高度100%
- 小尺寸 (`size="small"`)
- 斑马纹 (`stripe`)
- 固定列宽 (`table-layout="fixed"`)
- 行高48px，单元格无内边距
- 表头背景色 #f5f7fa

**表格列定义：**

| 列名 | 宽度 | 对齐 | 说明 |
|------|------|------|------|
| 复选框 | 50px | 居中 | 多选行 |
| 主机名 | min-width: 140px | - | 蓝色可点击链接，跳转详情 |
| IP地址 | min-width: 150px | - | 主IP + 内网IP（带颜色点） |
| 配置 | 110px | 居中 | CPU/内存，如 "4C/16G" |
| 环境 | 75px | 居中 | Tag标签，颜色区分 |
| 分组 | min-width: 120px | - | Tag标签，显示分组颜色 |
| 资源使用率 | min-width: 200px | 居中 | CPU/内存/磁盘百分比 |
| CPU趋势 | 100px | 居中 | MiniTrendChart 折线图 |
| 服务状态 | 100px | 居中 | ServiceStatusIcon 图标 |
| 告警 | 80px | 居中 | AlertBadge 徽章 |
| 操作 | 200px | 居中 | 固定右侧 |

**操作列按钮：**
1. **连接按钮** (绿色 #00A67D)
   - 带终端图标
   - 打开SSH连接

2. **更多操作** (...)
   - 下拉菜单项:
     - 刷新指标 (Agent运行时)
     - 监控详情 (Agent运行时)
     - 主机详情
     - 编辑
     - 部署 Agent (未安装时)
     - 重启 Agent (运行/离线时)
     - 卸载 Agent (运行/离线时)
     - ────────
     - 删除主机 (红色)

#### 分页组件

```vue
<div v-if="tableData.length > 0" class="p-12px border-t border-gray-200 flex justify-end bg-white">
```

**布局特点：**
- 仅在有数据时显示
- 右对齐 (`justify-end`)
- 上边框分隔线
- 白色背景

**分页配置：**
- 每页显示数量选项: [10, 20, 50, 100]
- 显示总数、页码切换、每页数量选择

## 样式规范

### 颜色方案

| 用途 | 颜色值 | 说明 |
|------|--------|------|
| 主操作按钮 | #00A67D | 绿色，用于创建等主操作 |
| 表头背景 | #f5f7fa | 浅灰 |
| 边框颜色 | #e5e7ed / #dcdfe6 | 浅灰 |
| 主机名链接 | #409EFF | 蓝色 |
| 环境标签 | 根据env类型 | prod=红色, test=黄色, dev=蓝色 |

### 尺寸规范

| 元素 | 尺寸 |
|------|------|
| 左侧分栏宽度 | 220px |
| 表格行高 | 48px |
| 按钮高度 | 32px (默认) / 24px (small) |
| 分栏间距 | 12px |
| 卡片内边距 | 12px |

### 图标使用

- **Material Design Icons** (`icon-mdi-*`)
- **Material Icons Round** (`icon-ic-round-*`)
- **自定义图标**: 通过 `component` 动态加载

## 交互特性

### 分组树交互

1. **点击节点**: 筛选该分组下的主机
2. **右键菜单**: 显示分组操作选项
3. **搜索分组**: 实时过滤分组树
4. **内联编辑**: 双击或右键重命名

### 表格交互

1. **点击主机名**: 跳转到主机详情页
2. **选择行**: 显示批量操作按钮
3. **连接按钮**: 打开SSH终端（新标签页）
4. **更多操作**: 下拉菜单显示所有可用操作

### 搜索功能

1. **分组搜索**: 实时过滤分组树
2. **主机搜索**: 支持按主机名/IP搜索
3. **Enter键**: 触发搜索
4. **筛选类型**: 下拉选择搜索字段

## 响应式设计

当前页面主要针对桌面端设计：
- 最小宽度建议: 1280px
- 左侧分栏固定宽度
- 右侧内容自适应

## 性能优化

1. **虚拟滚动**: 表格使用固定高度和虚拟化
2. **懒加载**: 图标组件按需加载
3. **防抖**: 搜索输入可添加防抖优化
4. **分页**: 限制每页最大数据量

## 维护要点

### 样式修改优先级

1. **优先使用UnoCSS原子类** (如 `p-12px`, `flex`, `gap-8px`)
2. **复杂样式使用 scoped style**
3. **避免内联样式** (除特殊情况如动态颜色)

### 状态管理

- 表格数据: `tableData`
- 选中行: `selectedIds`
- 分组数据: `groupTree`
- 加载状态: `loading`

### 事件处理

- 分组操作: `handleNodeClick`, `handleNodeContextMenu`
- 主机操作: `handleEdit`, `handleDelete`, `handleConnect`
- 搜索: `handleSearch`, `handleReset`

## 依赖组件

- **Element Plus**: ElCard, ElTree, ElTable, ElButton, ElDropdown等
- **VueUse**: 用于组合式API工具函数
- **自定义组件**:
  - MiniTrendChart (CPU趋势图)
  - ServiceStatusIcon (服务状态图标)
  - AlertBadge (告警徽章)
