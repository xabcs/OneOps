# 动态属性系统改造实施计划

## 一、项目概述

### 1.1 项目目标

将 OneOps 主机管理从固定字段改造为动态属性系统，实现：
- 管理员可通过管理页面自定义主机属性
- 用户创建主机时动态选择属性
- 无需修改代码即可扩展主机属性

### 1.2 项目范围

**包含：**
- 后端属性定义管理API
- 前端属性管理页面
- 主机表单改造（支持动态属性）
- 主机列表显示改造（显示属性值）
- 数据库迁移脚本

**不包含：**
- 其他资产类型（网络设备、存储设备）的动态属性改造
- 属性的权限控制（暂不支持按角色限制属性）
- 属性的历史版本管理

### 1.3 预期收益

| 收益项 | 说明 |
|--------|------|
| **灵活性提升** | 管理员可随时新增属性，无需开发者介入 |
| **开发效率** | 减少因属性变更导致的前后端代码修改 |
| **用户体验** | 不同客户可自定义属性，满足个性化需求 |
| **维护成本** | 属性变更只需配置，无需发版 |

### 1.4 潜在风险

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| **查询性能下降** | 属性查询需要JOIN，可能变慢 | 优化索引、添加缓存、限制属性数量 |
| **数据迁移风险** | 现有数据迁移可能出错 | 充分测试、保留原字段、分阶段迁移 |
| **用户学习成本** | 用户需要理解属性定义概念 | 提供文档、预置常用属性、引导教程 |
| **数据一致性** | 删除属性定义可能导致数据孤岛 | 软删除、级联删除、数据清理检查 |

## 二、实施步骤

### 阶段一：后端开发（3天）

#### 任务1.1：数据库设计与创建（0.5天）
**负责人：** 后端开发
**工作量：** 4小时

**任务清单：**
- [ ] 创建 `attribute_definitions` 表
- [ ] 创建 `server_attributes` 表
- [ ] 创建索引
- [ ] 创建预置属性定义数据

**验收标准：**
- 表结构创建成功
- 索引创建成功
- 预置数据插入成功

**SQL脚本：**
```sql
-- 见设计文档中的SQL脚本
```

#### 任务1.2：数据模型开发（0.5天）
**负责人：** 后端开发
**工作量：** 4小时

**任务清单：**
- [ ] 创建 `AttributeDefinition` 模型
- [ ] 创建 `ServerAttribute` 模型
- [ ] 实现模型方法（TableName等）

**验收标准：**
- 模型定义完成
- GORM标签正确
- 单元测试通过

#### 任务1.3：属性定义API开发（1天）
**负责人：** 后端开发
**工作量：** 8小时

**任务清单：**
- [ ] 创建 `AttributeController`
- [ ] 创建 `AttributeService`
- [ ] 实现属性定义CRUD接口
- [ ] 实现属性验证逻辑
- [ ] 添加权限控制

**API接口：**
```
GET    /api/attributes           # 获取属性列表
POST   /api/attributes           # 创建属性
PUT    /api/attributes/:id       # 更新属性
DELETE /api/attributes/:id       # 删除属性
GET    /api/attributes/:id       # 获取属性详情
```

**验收标准：**
- API接口功能正常
- 权限控制生效
- 属性验证逻辑正确

#### 任务1.4：主机属性API改造（1天）
**负责人：** 后端开发
**工作量：** 8小时

**任务清单：**
- [ ] 修改 `CreateServer` 支持 `attributes` 参数
- [ ] 修改 `UpdateServer` 支持 `attributes` 参数
- [ ] 修改 `GetServers` 预加载属性数据
- [ ] 修改 `GetServerByID` 预加载属性数据
- [ ] 实现按属性筛选主机功能
- [ ] 实现属性数据迁移逻辑

**验收标准：**
- 主机创建/更新时正确保存属性
- 主机查询时正确返回属性
- 属性筛选功能正常
- 数据迁移脚本可用

### 阶段二：前端开发（4天）

#### 任务2.1：属性管理页面（2天）
**负责人：** 前端开发
**工作量：** 16小时

**任务清单：**
- [ ] 创建属性管理页面 `src/views/attribute_management/index.vue`
- [ ] 实现属性列表表格
- [ ] 实现新增属性对话框
- [ ] 实现编辑属性对话框
- [ ] 实现删除确认对话框
- [ ] 实现属性分类筛选
- [ ] 实现属性类型验证

**页面路由：**
```typescript
{
  path: '/system/attributes',
  name: 'system_attributes',
  component: () => import('@/views/attribute_management/index.vue'),
  meta: {
    title: '属性管理',
    i18nKey: 'route.system_attributes',
    requiresAuth: true,
    permission: 'system:attributes'
  }
}
```

**验收标准：**
- 属性列表正常显示
- 新增/编辑/删除功能正常
- 属性分类筛选正常
- 表单验证正确

#### 任务2.2：主机表单改造（1.5天）
**负责人：** 前端开发
**工作量：** 12小时

**任务清单：**
- [ ] 修改主机表单，支持动态属性
- [ ] 实现属性动态渲染逻辑
- [ ] 实现属性值绑定
- [ ] 实现属性验证
- [ ] 优化表单布局（折叠面板）
- [ ] 保留原有固定字段兼容性

**关键代码：**
```typescript
// 动态渲染属性输入组件
function renderAttributeInput(attr: AttributeDefinition) {
  switch (attr.type) {
    case 'text': return h(ElInput, { ... })
    case 'select': return h(ElSelect, { ... })
    // ...
  }
}
```

**验收标准：**
- 属性动态渲染正确
- 属性值保存正确
- 属性验证生效
- 表单布局合理

#### 任务2.3：主机列表显示改造（0.5天）
**负责人：** 前端开发
**工作量：** 4小时

**任务清单：**
- [ ] 修改主机列表，显示属性值
- [ ] 实现属性值格式化显示
- [ ] 优化列表列配置
- [ ] 实现属性筛选功能

**验收标准：**
- 属性值正确显示
- 属性筛选功能正常
- 列表性能良好

### 阶段三：测试与优化（2天）

#### 任务3.1：功能测试（1天）
**负责人：** 测试+开发
**工作量：** 8小时

**测试用例：**

| 用例编号 | 测试场景 | 预期结果 |
|---------|---------|---------|
| TC-001 | 创建属性定义 | 属性创建成功，列表显示 |
| TC-002 | 编辑属性定义 | 属性更新成功，主机表单生效 |
| TC-003 | 删除属性定义 | 属性删除成功，数据清理 |
| TC-004 | 创建主机并填写属性 | 主机和属性都保存成功 |
| TC-005 | 编辑主机属性 | 属性更新成功 |
| TC-006 | 按属性筛选主机 | 筛选结果正确 |
| TC-007 | 必填属性验证 | 必填属性为空时提示错误 |
| TC-008 | 下拉选择验证 | 只能选择预定义选项 |
| TC-009 | 数字属性验证 | 只能输入数字 |
| TC-010 | 数据迁移 | 现有数据迁移成功 |

#### 任务3.2：性能测试（0.5天）
**负责人：** 测试+开发
**工作量：** 4小时

**测试场景：**
- 1000台主机，每台10个属性的查询性能
- 按属性筛选的查询性能
- 属性定义缓存效果验证

**性能指标：**
- 主机列表查询 < 500ms
- 属性筛选查询 < 300ms
- 属性管理页面加载 < 200ms

#### 任务3.3：Bug修复（0.5天）
**负责人：** 开发
**工作量：** 4小时

**任务清单：**
- [ ] 修复测试中发现的Bug
- [ ] 优化性能瓶颈
- [ ] 完善错误提示

### 阶段四：发布（1天）

#### 任务4.1：数据迁移（0.5天）
**负责人：** 后端开发+DBA
**工作量：** 4小时

**任务清单：**
- [ ] 准备数据迁移脚本
- [ ] 在测试环境验证迁移
- [ ] 在生产环境执行迁移
- [ ] 验证迁移结果

**迁移脚本：**
```sql
-- 1. 备份现有数据
CREATE TABLE servers_backup AS SELECT * FROM servers;

-- 2. 迁移业务系统
INSERT INTO server_attributes (server_id, attribute_id, attribute_key, attribute_value, category)
SELECT 
    s.id,
    (SELECT id FROM attribute_definitions WHERE key = 'business_system'),
    'business_system',
    bu.name,
    'system'
FROM servers s
LEFT JOIN business_units bu ON s.business_id = bu.id
WHERE s.business_id IS NOT NULL;

-- 3. 迁移机房机柜（类似逻辑）
-- ...
```

#### 任务4.2：发布准备（0.5天）
**负责人：** 项目经理+开发
**工作量：** 4小时

**任务清单：**
- [ ] 编写发布说明
- [ ] 准备用户培训材料
- [ ] 制定回滚方案
- [ ] 安排发布时间窗口

**发布清单：**
- [ ] 数据库迁移脚本
- [ ] 后端代码部署
- [ ] 前端代码部署
- [ ] 配置文件更新
- [ ] 缓存清理

## 三、时间规划

### 3.1 总体时间线

```
Week 1: 后端开发
├─ Day 1-2: 数据库设计 + 模型开发 + API开发
└─ Day 3:   主机属性API改造

Week 2: 前端开发
├─ Day 1-2: 属性管理页面
├─ Day 3:   主机表单改造
└─ Day 4:   主机列表显示改造

Week 3: 测试与发布
├─ Day 1:   功能测试
├─ Day 2:   性能测试 + Bug修复
└─ Day 3-4: 数据迁移 + 发布
```

### 3.2 里程碑

| 里程碑 | 日期 | 交付物 | 验收标准 |
|--------|------|--------|----------|
| M1: 后端API完成 | Day 3 | 属性定义API + 主机属性API | API功能正常 |
| M2: 前端页面完成 | Day 7 | 属性管理页面 + 主机表单 | 页面功能正常 |
| M3: 测试完成 | Day 9 | 测试报告 | 用例全部通过 |
| M4: 发布完成 | Day 10 | 生产环境上线 | 系统稳定运行 |

## 四、资源需求

### 4.1 人力资源

| 角色 | 人数 | 工作量 | 说明 |
|------|------|--------|------|
| 后端开发 | 1 | 3天 | Go开发 |
| 前端开发 | 1 | 4天 | Vue开发 |
| 测试工程师 | 1 | 2天 | 功能+性能测试 |
| DBA | 0.5 | 0.5天 | 数据迁移指导 |

### 4.2 环境资源

| 环境 | 用途 | 要求 |
|------|------|------|
| 开发环境 | 开发调试 | 与现有开发环境一致 |
| 测试环境 | 测试验证 | 与生产环境配置一致 |
| 生产环境 | 正式运行 | 需要停机维护窗口 |

## 五、风险管理

### 5.1 风险识别与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| **数据迁移失败** | 中 | 高 | 1. 充分测试迁移脚本<br>2. 保留原字段作为备份<br>3. 准备回滚方案 |
| **性能下降** | 中 | 中 | 1. 优化索引<br>2. 添加缓存<br>3. 限制属性数量 |
| **用户不适应** | 高 | 低 | 1. 提供培训文档<br>2. 预置常用属性<br>3. 引导教程 |
| **属性定义冲突** | 低 | 中 | 1. 属性键唯一性约束<br>2. 管理员权限控制 |
| **开发延期** | 中 | 中 | 1. 合理预留时间<br>2. 分阶段发布 |

### 5.2 回滚方案

**触发条件：**
- 数据迁移失败
- 严重Bug无法快速修复
- 性能严重下降

**回滚步骤：**
1. 停止新版本服务
2. 恢复旧版本代码
3. 恢复数据库备份
4. 验证系统功能
5. 通知用户回滚

**回滚时间：** 30分钟

## 六、质量保证

### 6.1 代码规范

**后端规范：**
- 遵循 Go 代码规范
- 使用 gorm.io ORM
- 错误处理要完整
- 添加必要的注释

**前端规范：**
- 遵循 Vue 3 + TypeScript 规范
- 使用 Composition API
- 组件命名语义化
- 添加必要的注释

### 6.2 测试策略

**单元测试：**
- 后端：覆盖率 > 70%
- 前端：关键组件测试

**集成测试：**
- API接口测试
- 端到端功能测试

**性能测试：**
- 查询性能测试
- 并发访问测试

### 6.3 验收标准

**功能验收：**
- [ ] 属性管理功能完整
- [ ] 主机属性保存正常
- [ ] 属性筛选功能正常
- [ ] 数据迁移无丢失

**性能验收：**
- [ ] 主机列表查询 < 500ms
- [ ] 属性筛选查询 < 300ms
- [ ] 属性管理页面 < 200ms

**兼容性验收：**
- [ ] 现有主机数据迁移成功
- [ ] 原有功能不受影响
- [ ] 浏览器兼容性良好

## 七、沟通计划

### 7.1 干系人沟通

| 干系人 | 沟通内容 | 沟通方式 | 时机 |
|--------|---------|---------|------|
| 产品经理 | 功能需求、验收 | 会议 | 需求确认、验收 |
| 开发团队 | 技术方案、进度 | 站会 | 每日 |
| 测试团队 | 测试用例、Bug | 工具 | 测试阶段 |
| 最终用户 | 新功能使用 | 文档+培训 | 发布前 |

### 7.2 培训计划

**培训内容：**
1. 动态属性系统介绍
2. 属性管理页面使用
3. 主机创建流程变化
4. 常见问题处理

**培训方式：**
- 在线文档
- 视频教程
- 在线答疑

## 八、发布后支持

### 8.1 监控指标

**系统监控：**
- API响应时间
- 数据库查询性能
- 错误率

**业务监控：**
- 属性创建数量
- 主机属性填充率
- 属性筛选使用频率

### 8.2 反馈收集

**收集方式：**
- 用户反馈入口
- 使用情况分析
- 定期回访

**处理流程：**
1. 记录反馈
2. 分类整理
3. 评估优先级
4. 纳入迭代计划

## 九、后续优化

### 9.1 功能增强

- [ ] 属性分组管理
- [ ] 属性依赖关系
- [ ] 属性模板（快速应用一组属性）
- [ ] 属性导入导出
- [ ] 属性使用统计

### 9.2 性能优化

- [ ] 属性值缓存
- [ ] 查询结果缓存
- [ ] 数据库读写分离
- [ ] 属性数据分表

### 9.3 体验优化

- [ ] 属性推荐（智能推荐常用属性）
- [ ] 属性拖拽排序
- [ ] 属性快捷填充
- [ ] 批量属性编辑

## 十、总结

### 10.1 成功标准

**项目成功定义：**
- ✅ 管理员可独立管理属性，无需开发者介入
- ✅ 用户可正常使用动态属性功能
- ✅ 系统性能满足要求
- ✅ 数据迁移无丢失

**预期收益：**
- 属性扩展效率提升 90%（从几天到几分钟）
- 开发工作量减少 80%（无需修改代码）
- 用户满意度提升（个性化属性支持）

### 10.2 关键成功因素

1. **充分测试**：确保数据迁移成功
2. **性能优化**：避免查询性能下降
3. **用户培训**：降低学习成本
4. **渐进式迁移**：保留兼容性，减少风险

### 10.3 项目承诺

**交付内容：**
- 完整的动态属性系统
- 属性管理页面
- 改造后的主机管理功能
- 数据迁移脚本
- 完整的文档

**不包含内容：**
- 其他资产类型的动态属性
- 属性权限控制
- 属性历史版本

**质量承诺：**
- 核心功能无严重Bug
- 性能指标达标
- 数据安全无丢失

---

**项目启动日期：** 待定
**预计完成日期：** 启动后10个工作日
**项目经理：** 待定
**技术负责人：** 待定
