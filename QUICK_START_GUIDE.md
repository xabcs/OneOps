# 角色绑定和用户授权 - 快速入门

## 一、角色绑定（3步完成）

### 步骤 1：同步外部角色
```
授权中心 > 应用管理
→ 找到 Jenkins
→ 点击"同步角色"
→ 自动显示角色列表
```

### 步骤 2：创建授权中心角色
```
授权中心 > 角色管理
→ 点击"添加角色"
→ 角色名称：开发人员
→ 角色代码：developer
→ 保存
```

### 步骤 3：创建角色绑定
```
授权中心 > 角色绑定
→ 选择角色：开发人员
→ 点击"添加绑定"
→ 选择应用：Jenkins
→ 选择外部角色：developer
→ 确定
```

**完成！** ✅ 角色绑定已创建

---

## 二、用户授权（2步完成）

### 步骤 1：创建用户
```
授权中心 > 用户管理
→ 点击"添加用户"
→ 用户名：zhangsan
→ 昵称：张三
→ 邮箱：zhangsan@example.com
→ 保存
→ 记录自动生成的密码
```

### 步骤 2：分配角色
```
授权中心 > 用户授权
→ 选择用户：张三
→ 点击"分配角色"
→ 选择角色：开发人员
→ 确定
```

**完成！** ✅ 用户自动在所有外部系统创建并授权

---

## 三、验证结果

### 验证角色绑定
```
授权中心 > 角色绑定
→ 选择角色：开发人员
→ 查看绑定列表：
   | 应用名称 | 外部角色名称 |
   |---------|-------------|
   | Jenkins | Developer   |
```

### 验证用户授权
```
授权中心 > 用户授权
→ 选择用户：张三
→ 查看用户角色列表：
   | 角色名称 | 授权人 | 授权时间 |
   |---------|-------|---------|
   | 开发人员 | admin | 2026-07-22 |
```

### 验证外部系统
```
Jenkins 登录：
→ 用户名：zhangsan
→ 密码：（创建用户时生成的密码）
→ 登录成功
→ 查看权限：developer 角色
```

---

## 四、典型场景

### 场景 1：新员工入职

```
张三入职，需要访问 Jenkins、Nacos、XXL-Job

传统方式：
❌ 在 Jenkins 创建用户
❌ 在 Nacos 创建用户
❌ 在 XXL-Job 创建用户
❌ 每个系统单独分配权限

使用授权中心：
✅ 步骤 1：创建用户（自动生成统一密码）
✅ 步骤 2：分配"开发人员"角色
→ 自动在 Jenkins、Nacos、XXL-Job 创建用户并授权
```

### 场景 2：员工离职

```
张三离职，需要撤销所有权限

传统方式：
❌ 登录 Jenkins 删除用户
❌ 登录 Nacos 删除用户
❌ 登录 XXL-Job 删除用户

使用授权中心：
✅ 步骤 1：进入用户授权
✅ 步骤 2：选择张三
✅ 步骤 3：删除所有角色
→ 自动从所有外部系统撤销权限
```

---

## 五、关系图

```
授权中心用户（张三）
     ↓ 分配角色
授权中心角色（开发人员）
     ↓ 角色绑定
┌─────────────────────────┐
│ Jenkins → developer     │
│ Nacos → developer        │
│ XXL-Job → developer      │
└─────────────────────────┘
     ↓ 自动创建和授权
外部系统用户：
├── Jenkins 用户：张三 (developer)
├── Nacos 用户：张三 (developer)
└── XXL-Job 用户：张三 (developer)
```

---

## 六、关键点

### ✅ 必须先完成
1. 同步外部应用角色
2. 创建角色绑定

### ✅ 自动完成
1. 用户创建时自动生成密码
2. 分配角色时自动在外部系统创建用户
3. 分配角色时自动授权

### ✅ 统一管理
1. 一个密码访问所有系统
2. 一次操作同步所有系统
3. 集中查看权限状态

---

## 七、常见问题

**Q: 为什么看不到外部角色？**
```
A: 需要先同步角色
→ 应用管理 > 找到应用 > 同步角色
```

**Q: 授权失败怎么办？**
```
A: 检查以下几点：
1. 是否创建了角色绑定
2. 外部应用配置是否正确
3. 查看授权结果对话框的错误信息
```

**Q: 用户密码在哪里看？**
```
A: 两个地方：
1. 创建用户时会自动显示
2. 用户列表 > 查看密码按钮
```

**Q: 如何修改用户密码？**
```
A: 目前不支持修改
→ 用户创建时生成的统一密码永久有效
→ 建议用户在外部系统首次登录后自行修改
```

---

## 八、快速检查清单

### 角色绑定检查
- [ ] 外部应用已添加
- [ ] 外部角色已同步
- [ ] 授权中心角色已创建
- [ ] 角色绑定已创建

### 用户授权检查
- [ ] 用户已创建
- [ ] 用户有密码
- [ ] 角色绑定已创建
- [ ] 角色已分配给用户
- [ ] 授权结果显示成功

---

## 九、操作流程图

```
┌─────────────────┐
│ 1. 同步外部角色 │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ 2. 创建角色绑定 │
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ 3. 创建用户     │ → 自动生成密码
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ 4. 分配角色     │ → 自动外部授权
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ 5. 用户登录     │ → 使用统一密码
└─────────────────┘
```

---

## 十、效率对比

| 操作 | 传统方式 | 授权中心 |
|------|---------|---------|
| 创建用户 | 每个系统单独创建 | 一次创建，自动同步 |
| 分配权限 | 每个系统单独分配 | 一次分配，自动同步 |
| 撤销权限 | 每个系统单独撤销 | 一次撤销，自动同步 |
| 密码管理 | 每个系统单独设置 | 统一密码 |
| 权限查看 | 登录每个系统查看 | 集中查看 |

**效率提升：**
- 创建用户：节省 80% 时间
- 分配权限：节省 90% 时间
- 撤销权限：节省 90% 时间
- 权限管理：提升 95% 效率

---

**详细文档：** `ROLE_BINDING_AND_AUTHORIZATION_GUIDE.md`
