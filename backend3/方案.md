# OneOps Backend3 重构方案

## 一、目标

将 `backend/`（技术层扁平结构）重构为 `backend3/`（技术层 + 功能模块子目录），实现：

- 架构统一：model/repository/service/controller 四层均按功能模块分子目录
- 无循环依赖
- 功能完整不丢失
- 消除全局 db 变量，统一构造函数注入

## 二、最终目录结构

backend3/
├── cmd/api/
│   └── main.go                              # 入口 + 依赖组装 + 路由注册
│
├── model/                                   # 纯struct，零业务逻辑
│   ├── system/          # package modelsystem
│   │   ├── user.go      # User
│   │   ├── role.go      # Role
│   │   ├── menu.go      # Menu
│   │   ├── permission.go # Permission, RolePermission, UserPermission, PermissionLog
│   │   ├── api_resource.go
│   │   └── attribute.go # AttributeDefinition（ServerAttributes 改为 gorm:"-"）
│   ├── cmdb/            # package modelcmdb
│   │   ├── server.go    # Server, BusinessUnit, ServerRoom, Cabinet
│   │   ├── tag.go       # ServerTag, ServerTagRelation, ServerGroup(+Relation)
│   │   ├── credential.go # SSHCredential, CloudServer, ServerCredential
│   │   ├── attribute.go # ServerAttribute（Definition → modelsystem.AttributeDefinition）
│   │   ├── bastion.go   # BastionSession/Command/FileTransfer/Approval, AssetAccessPolicy
│   │   └── agent.go     # AgentVersion, AgentUpgradeTask, DiskPartition, AssetChange
│   ├── k8s/             # package modelk8s
│   │   ├── cluster.go   # K8sCluster, ClusterRoleBinding（User→modelsystem.User）
│   │   ├── terminal.go  # K8sSession, K8sCommand
│   │   └── diagnostic.go
│   ├── audit/           # package modelaudit
│   │   └── log.go
│   └── authorization/   # package modelauth
│       ├── application.go
│       ├── auth_user.go
│       ├── group_binding.go
│       ├── permission_mapping.go
│       ├── identity_mapping.go
│       ├── authorization_rule.go
│       └── operation_log.go
│
├── repository/          # 数据库操作层（统一 db 注入）
│   ├── system/   {user, role, menu, permission, attribute}_repo.go
│   ├── cmdb/     {server, credential, bastion, agent}_repo.go
│   ├── k8s/      {cluster, diagnostic}_repo.go
│   ├── audit/    audit_repo.go
│   └── authorization/ application_repo.go
│
├── service/             # 业务逻辑层（统一 repo 注入）
│   ├── system/   {auth, rbac, permission, casbin_api, route_gen}.go
│   ├── cmdb/     {cmdb, bastion, agent}.go    # agent → monitoring 单向
│   ├── k8s/      {cluster, resource, identity, client_pool, diagnostic}.go
│   ├── audit/    audit.go
│   ├── monitoring/ {monitoring, alert_rule, metrics, data_backfill, lifecycle}.go
│   │              notification/{email, wechat}.go
│   └── authorization/ application_permission.go
│                      adapter/{factory, gitlab, jenkins, jumpserver, generic}.go
│
├── controller/          # 瘦controller（统一 service 注入）
│   ├── system/   {auth, user, role, menu, route, permission}.go
│   ├── cmdb/     {cmdb, bastion, attribute, agent, websocket}.go  # SSH终端并入
│   ├── k8s/      {cluster, resource, permission, diagnostic, terminal}.go  # Pod终端并入
│   ├── audit/    audit.go
│   ├── monitoring/ {monitoring, websocket}.go
│   └── authorization/ application_permission.go
│
├── routes/              # 扁平文件（每模块一个）
│   ├── router.go        # SetupRoutes 总入口
│   ├── system.go
│   ├── cmdb.go
│   ├── k8s.go
│   ├── audit.go
│   ├── monitoring.go
│   └── authorization.go
│
├── config/              # 配置（原样保留）
├── pkg/
│   ├── database/        # GORM连接 + Redis（从services/db.go+redis.go提取）
│   ├── logger/
│   ├── middleware/      # Auth, Permission, Audit, CORS, ErrorHandler
│   ├── dto/             # 请求参数struct + validator
│   ├── errors/          # 错误码
│   └── utils/           # JWT, 加密, 密码, 分页, context
└── go.mod               # module oneops/backend3

## 三、依赖方向（无环）

```
model:
  authorization, audit → (无外部依赖)
  system → (无外部依赖，基础层)
  cmdb → system (ServerAttribute.Definition → modelsystem.AttributeDefinition)
  k8s → system (ClusterRoleBinding.User/Role → modelsystem.User/Role)

repository → model (单向)
service → repository (单向)
controller → service (单向)
routes → controller (单向)
pkg/middleware → service (单向，service 不反向依赖)
```

## 四、代码改造清单

| # | 改造项                         | 详情                                                                        |
| - | ------------------------------ | --------------------------------------------------------------------------- |
| 1 | AttributeDefinition 关联标签   | `ServerAttributes` 从 `gorm:"foreignKey:AttributeID"` 改为 `gorm:"-"` |
| 2 | controller/route.go 下沉       | 路由生成算法移到 service/system/route_gen.go                                |
| 3 | controller/diagnostic.go 下沉  | Java分析+HTTP调用移到 service/k8s/diagnostic.go                             |
| 4 | controller/cmdb.go 拆分        | Agent管理拆到 controller/cmdb/agent.go                                      |
| 5 | controller/{menu,role,user}.go | 直接DB操作下沉到 repository/system/                                         |
| 6 | middleware/permission_db.go    | 改为调用 service 方法                                                       |
| 7 | 全局 var db 消除               | 统一改为 repository 构造函数注入                                            |
| 8 | container 删除                 | main.go 中手动组装依赖链                                                    |
| 9 | handler 并入 controller        | WebSocket 处理器移到 controller/{cmdb,k8s}/                                 |

## 五、实施步骤

### 步骤1：初始化 backend3

- 复制 backend → backend3
- 修改 go.mod: `module oneops/backend3`
- 创建所有分层子目录

### 步骤2：迁移 model 层

- models/ → model/{system,cmdb,k8s,audit,authorization}/
- 按模块拆分文件，修改 package 名
- 改造 AttributeDefinition 标签
- 跨模块引用加 import

### 步骤3：迁移 pkg 共享层

- errors/ → pkg/errors/
- utils/ → pkg/utils/
- config/ 保留
- logger/ → pkg/logger/
- dto/ → pkg/dto/
- middleware/ → pkg/middleware/
- services/db.go → pkg/database/
- services/redis.go → pkg/database/

### 步骤4：迁移 repository 层

- 从 services/ 中提取纯 CRUD 操作到 repository/{模块}/
- 统一 `NewXxxRepository(db *gorm.DB)` 构造函数

### 步骤5：迁移 service 层

- services/ → service/{system,cmdb,k8s,audit,monitoring,authorization}/
- 统一 `NewXxxService(repo)` 构造函数
- 下沉胖 controller 逻辑（route_gen, diagnostic）

### 步骤6：迁移 controller 层

- controller/ → controller/{模块}/
- handler/ 并入 controller/{cmdb,k8s}/
- 统一 `NewXxxController(service)` 构造函数

### 步骤7：迁移 routes + 入口

- routes/ 扁平文件
- main.go → cmd/api/main.go
- 手动组装依赖链

### 步骤8：编译修复

- 全局替换 import 路径
- go build ./... 迭代修复
