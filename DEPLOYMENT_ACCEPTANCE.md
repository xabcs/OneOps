# OneOps 按钮级权限管理系统 - 部署和验收文档

## 部署指南

### 1. 数据库部署

#### 步骤1：备份数据库
```bash
# 备份现有数据库
mysqldump -u root -p msre > backup_$(date +%Y%m%d_%H%M%S).sql
```

#### 步骤2：执行数据库迁移
```bash
# 进入后端目录
cd backend

# 创建数据库表（如果不存在）
mysql -u root -p < migrations/permission_tables.sql

# 插入初始权限数据
mysql -u root -p msre < migrations/seed_permissions.sql
```

#### 步骤3：验证数据库
```sql
-- 验证表是否创建成功
SELECT COUNT(*) FROM permissions;
SELECT COUNT(*) FROM role_permissions;
SELECT COUNT(*) FROM user_permissions;

-- 验证权限数据
SELECT module, level, COUNT(*) as count
FROM permissions
GROUP BY module, level;
```

### 2. 后端部署

#### 步骤1：安装 Casbin 依赖
```bash
cd backend
go get github.com/casbin/casbin/v2
go get github.com/casbin/gorm-adapter/v3
```

#### 步骤2：编译后端
```bash
# 开发模式
go run main.go

# 生产模式
go build -o oneops
./oneops
```

#### 步骤3：验证后端 API
```bash
# 测试权限 API
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:8082/api/v1/permissions/tree

# 测试用户权限 API
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:8082/api/v1/user/permissions
```

### 3. 前端部署

#### 步骤1：安装前端依赖
```bash
cd frontend
npm install
```

#### 步骤2：构建前端
```bash
# 开发模式
npm run dev

# 生产模式
npm run build
```

#### 步骤3：验证前端
```bash
# 访问前端
http://localhost:5173

# 检查权限控制是否生效
# 1. 登录系统
# 2. 查看用户管理页面
# 3. 验证按钮是否根据权限正确显示/隐藏
```

## 验收测试

### 功能验收测试

#### 测试用例1：权限数据初始化
**测试目标**: 验证权限数据是否正确初始化

**测试步骤**:
1. 连接数据库，查询 permissions 表
2. 验证是否有基础权限数据（用户管理、角色管理、权限管理）
3. 验证权限树结构是否正确（parent_id 关系）

**预期结果**:
- ✅ permissions 表中至少有30+条基础权限记录
- ✅ 权限分为 menu 和 button 两个级别
- ✅ 权限编码符合 `module.resource.action` 格式
- ✅ 权限树结构完整

#### 测试用例2：权限服务功能
**测试目标**: 验证后端权限服务是否正常工作

**测试步骤**:
1. 调用 `/api/v1/permissions/tree` 获取权限树
2. 调用 `/api/v1/roles/1/permissions` 获取角色权限
3. 调用 `/api/v1/user/permissions` 获取用户权限
4. 测试权限分配接口

**预期结果**:
- ✅ 权限树返回完整的层级结构
- ✅ 角色权限返回正确
- ✅ 用户权限列表正确
- ✅ 权限分配功能正常

#### 测试用例3：前端权限指令
**测试目标**: 验证 v-permission 指令是否正常工作

**测试步骤**:
1. 使用不同权限的用户登录系统
2. 访问用户管理页面
3. 观察按钮是否根据权限正确显示/隐藏

**预期结果**:
- ✅ 有权限的用户能看到对应的按钮
- ✅ 无权限的用户看不到对应的按钮或按钮被禁用
- ✅ 超级管理员能看到所有按钮

#### 测试用例4：HTTP 拦截器权限检查
**测试目标**: 验证 HTTP 拦截器是否正确拦截无权限请求

**测试步骤**:
1. 使用无权限的用户尝试执行敏感操作
2. 观察请求是否被拦截
3. 检查错误提示是否友好

**预期结果**:
- ✅ 无权限的请求被正确拦截
- ✅ 返回 403 Forbidden 状态码
- ✅ 显示友好的权限不足提示

#### 测试用例5：权限分配功能
**测试目标**: 验证管理员能否为角色分配权限

**测试步骤**:
1. 使用管理员账号登录
2. 进入角色权限分配页面
3. 选择角色并分配权限
4. 保存权限分配
5. 使用该角色用户重新登录，验证权限是否生效

**预期结果**:
- ✅ 管理员可以访问权限管理页面
- ✅ 权限树正确显示，支持勾选
- ✅ 保存权限功能正常
- ✅ 角色用户的权限正确更新

### 性能验收测试

#### 测试用例6：权限检查性能
**测试目标**: 验证权限检查不影响系统性能

**测试基准**:
- 权限检查响应时间 < 50ms
- 权限树加载时间 < 500ms
- 系统整体性能无明显下降

**测试步骤**:
1. 使用性能测试工具（如 JMeter）
2. 模拟大量权限检查请求
3. 监控响应时间和系统资源

**预期结果**:
- ✅ 权限检查响应时间符合基准
- ✅ 权限树加载时间符合基准
- ✅ 系统整体性能无明显下降

### 安全验收测试

#### 测试用例7：绕过前端控制
**测试目标**: 验证绕过前端控制的安全性

**测试步骤**:
1. 使用浏览器开发者工具，手动添加被隐藏的按钮
2. 点击按钮，尝试执行操作
3. 观察后端是否正确拦截

**预期结果**:
- ✅ 后端正确拦截无权限的请求
- ✅ 返回权限不足错误
- ✅ 操作未被执行

#### 测试用例8：权限提升攻击防护
**测试目标**: 验证权限提升攻击是否被防护

**测试步骤**:
1. 尝试通过 API 直接修改权限
2. 尝试将自己提升为管理员
3. 尝试访问管理员专属功能

**预期结果**:
- ✅ 权限提升攻击被正确拦截
- ✅ 敏感操作有完整的审计日志
- ✅ 权限验证严格执行

## 验收检查清单

### 功能验收
- [ ] 管理员可以通过界面管理权限
- [ ] 管理员可以为角色分配按钮级权限
- [ ] 用户根据权限看到对应的菜单和按钮
- [ ] 无权限的按钮被正确隐藏或禁用
- [ ] HTTP 拦截器正确拦截无权限请求
- [ ] 后端 API 正确验证权限
- [ ] 权限树结构正确显示
- [ ] 权限分配功能正常工作

### 性能验收
- [ ] 权限检查响应时间 < 50ms
- [ ] 权限树加载时间 < 500ms
- [ ] 系统整体性能无明显下降
- [ ] 权限缓存机制正常工作

### 安全验收
- [ ] 前端权限控制无法被绕过
- [ ] 后端权限验证严格执行
- [ ] 权限提升攻击防护有效
- [ ] 敏感操作有完整的审计日志
- [ ] 权限验证失败有安全日志

### 兼容性验收
- [ ] 现有功能正常工作
- [ ] 现有角色权限正确迁移
- [ ] 新旧权限系统可以并存
- [ ] 支持回滚到旧权限系统

### 文档验收
- [ ] 用户使用手册完整
- [ ] API 接口文档完整
- [ ] 部署文档完整
- [ ] 权限编码规范文档完整

## 常见问题和解决方案

### 问题1：权限树显示不完整
**原因**: 权限数据的 parent_id 关系不正确

**解决方案**:
```sql
-- 重新更新权限树的父级关系
UPDATE permissions p
JOIN permissions parent ON parent.code = 'system.user.menu'
SET p.parent_id = parent.id
WHERE p.code LIKE 'system.user.%' AND p.code != 'system.user.menu';
```

### 问题2：权限检查不生效
**原因**: 用户权限未正确加载或缓存

**解决方案**:
1. 检查用户是否正确登录
2. 检查 token 是否有效
3. 清除浏览器缓存，重新登录
4. 检查后端权限服务是否正常运行

### 问题3：Casbin 策略不同步
**原因**: 数据库权限表和 Casbin 策略不一致

**解决方案**:
1. 重新同步所有角色到 Casbin
2. 检查 Casbin 适配器配置
3. 查看 Casbin 日志排查问题

### 问题4：前端权限指令不工作
**原因**: 权限指令未正确注册或权限数据未加载

**解决方案**:
1. 检查 main.ts 中是否注册了权限指令
2. 检查用户权限数据是否正确加载
3. 在浏览器控制台检查权限检查逻辑

### 问题5：数据库外键约束错误
**原因**: 删除权限时违反外键约束

**解决方案**:
1. 先删除相关的角色权限关联
2. 再删除权限记录
3. 或者临时禁用外键约束

## 部署脚本

### 自动化部署脚本
```bash
#!/bin/bash
# deploy.sh - 自动化部署脚本

set -e

echo "开始部署 OneOps 权限系统..."

# 备份数据库
echo "备份数据库..."
mysqldump -u root -p123456 msre > backup_$(date +%Y%m%d_%H%M%S).sql

# 执行数据库迁移
echo "执行数据库迁移..."
cd backend
mysql -u root -p123456 msre < migrations/permission_tables.sql
mysql -u root -p123456 msre < migrations/seed_permissions.sql

# 安装依赖
echo "安装后端依赖..."
go get github.com/casbin/casbin/v2
go get github.com/casbin/gorm-adapter/v3

# 编译后端
echo "编译后端..."
go build -o oneops

# 安装前端依赖
echo "安装前端依赖..."
cd ../frontend
npm install

# 构建前端
echo "构建前端..."
npm run build

echo "部署完成！"
echo "请启动后端服务：cd ../backend && ./oneops"
echo "前端静态文件已生成到 dist/ 目录"
```

## 验收报告模板

### 验收报告
**项目名称**: OneOps 按钮级权限管理系统
**验收日期**: YYYY-MM-DD
**验收人员**: ___________
**验收环境**: 开发环境 / 测试环境 / 生产环境

#### 功能验收结果
| 功能项 | 验收结果 | 备注 |
|--------|----------|------|
| 权限数据初始化 | ⬜ 通过 / ⬜ 不通过 | |
| 权限服务功能 | ⬜ 通过 / ⬜ 不通过 | |
| 前端权限指令 | ⬜ 通过 / ⬜ 不通过 | |
| HTTP 拦截器 | ⬜ 通过 / ⬜ 不通过 | |
| 权限分配功能 | ⬜ 通过 / ⬜ 不通过 | |
| 权限管理界面 | ⬜ 通过 / ⬜ 不通过 | |

#### 性能验收结果
| 性能指标 | 目标值 | 实际值 | 结果 |
|----------|--------|--------|------|
| 权限检查响应时间 | < 50ms | _____ | ⬜ 通过 / ⬜ 不通过 |
| 权限树加载时间 | < 500ms | _____ | ⬜ 通过 / ⬜ 不通过 |
| 系统整体性能 | 无明显下降 | _____ | ⬜ 通过 / ⬜ 不通过 |

#### 安全验收结果
| 安全项 | 验收结果 | 备注 |
|--------|----------|------|
| 前端绕过防护 | ⬜ 通过 / ⬜ 不通过 | |
| 后端权限验证 | ⬜ 通过 / ⬜ 不通过 | |
| 权限提升防护 | ⬜ 通过 / ⬜ 不通过 | |
| 审计日志完整性 | ⬜ 通过 / ⬜ 不通过 | |

#### 验收结论
- [ ] 功能验收通过
- [ ] 性能验收通过
- [ ] 安全验收通过
- [ ] **总体验收通过 / 不通过**

#### 验收意见
（记录验收过程中的问题和改进建议）

---

**验收人员签名**: ___________
**日期**: YYYY-MM-DD