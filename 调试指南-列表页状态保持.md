# 列表页状态保持 - 调试指南

## 如何查看调试日志

### 方法1：浏览器控制台（推荐）

1. **打开浏览器开发者工具**
   - Chrome/Edge: 按 `F12` 或 `Ctrl+Shift+I`
   - Firefox: 按 `F12` 或 `Ctrl+Shift+K`

2. **切换到 Console 标签页**

3. **清空控制台**
   - 点击 🚫 图标或按 `Ctrl+L`

### 方法2：查找日志

在控制台中使用搜索框过滤日志：
- 搜索 `[Deployment列表]` 查看 Deployment 列表页日志
- 搜索 `[Deployment详情]` 查看 Deployment 详情页日志
- 搜索 `[Pod列表]` 查看 Pod 列表页日志
- 搜索 `[Pod详情]` 查看 Pod 详情页日志
- 搜索 `[ActionBar]` 查看操作栏日志

---

## 测试步骤

### 测试 Deployment 列表页

1. **打开 Deployment 列表页**
   ```
   访问: http://localhost:3000/k8s/workloads
   ```

2. **选择集群和命名空间**
   - 选择集群：如 `cluster-1`
   - 选择命名空间：如 `default`

3. **查看控制台日志**
   应该看到类似输出：
   ```
   [Deployment列表] 开始恢复状态...
   [Deployment列表] sessionStorage 内容: null
   [Deployment列表] 无保存状态，执行正常加载
   ```

4. **点击某个 Deployment 的"详情"按钮**

5. **查看保存状态的日志**
   应该看到：
   ```
   [Deployment列表] 保存状态到 sessionStorage: {
     clusterId: 1,
     namespace: "default",
     listPath: "/k8s/workloads"
   }
   ```

6. **在详情页点击返回箭头 `←`**

7. **查看详情页和列表页的日志**
   详情页：
   ```
   [Deployment详情] 计算 backPath...
   [Deployment详情] sessionStorage 内容: {"clusterId":1,"namespace":"default","listPath":"/k8s/workloads"}
   [Deployment详情] 解析后的状态: {clusterId: 1, namespace: "default", listPath: "/k8s/workloads"}
   [Deployment详情] 返回路径: /k8s/workloads
   [ActionBar] 点击返回按钮，backPath: /k8s/workloads
   ```

   列表页：
   ```
   [Deployment列表] 路由变化: {oldPath: "/k8s/resources/deployments/detail", newPath: "/k8s/workloads"}
   [Deployment列表] 检测到从详情页返回，触发状态恢复
   [Deployment列表] 开始恢复状态...
   [Deployment列表] sessionStorage 内容: {"clusterId":1,"namespace":"default","listPath":"/k8s/workloads"}
   [Deployment列表] 解析后的状态: {clusterId: 1, namespace: "default", listPath: "/k8s/workloads"}
   [Deployment列表] 已设置 clusterId: 1, namespace: default
   [Deployment列表] 已清除 sessionStorage
   [Deployment列表] 开始加载命名空间和数据...
   ```

---

## 预期行为

### ✅ 正常工作流程

1. **列表页加载**
   - 如果 sessionStorage 中有保存的状态 → 恢复状态
   - 如果 sessionStorage 中无状态 → 正常加载（选择第一个集群）

2. **点击详情**
   - 当前选择的 `clusterId` 和 `namespace` 被保存到 sessionStorage
   - 同时保存列表页的路径（用于返回）

3. **点击返回**
   - 详情页读取 sessionStorage 中的 `listPath`
   - 跳转到正确的列表页路径
   - 列表页 `onMounted` 被触发
   - 从 sessionStorage 恢复 `clusterId` 和 `namespace`
   - 自动加载数据

---

## 故障排查

### 问题1：状态没有恢复

**症状**：返回列表页后，集群和命名空间被重置为默认值

**检查步骤**：
1. 查看控制台是否有 `[Deployment列表] 开始恢复状态...` 日志
2. 查看 `sessionStorage 内容` 日志是否显示保存的数据
3. 查看是否有 `[Deployment列表] 已设置 clusterId` 日志

**可能原因**：
- ❌ sessionStorage 中没有数据（检查保存逻辑是否执行）
- ❌ JSON 解析失败（检查数据格式）
- ❌ 组件没有重新挂载（检查路由跳转是否使用 `router.push`）

---

### 问题2：返回到错误的列表页

**症状**：从 Deployment 详情页返回后，跳转到错误的页面

**检查步骤**：
1. 查看 `[Deployment详情] 返回路径:` 日志
2. 确认 `listPath` 是否正确保存

**可能原因**：
- ❌ `listPath` 设置错误
- ❌ 详情页没有读取到 sessionStorage

---

### 问题3：onMounted 没有触发

**症状**：返回后页面不刷新，状态没有恢复

**检查步骤**：
1. 查看是否有 `[Deployment列表] 路由变化:` 日志
2. 检查 `oldPath` 和 `newPath` 是否符合预期

**可能原因**：
- ❌ 使用了 `router.back()` 而不是 `router.push()`
- ❌ 路由路径配置错误

---

## 日志分析

### 正常情况的日志序列

```
# 第一次进入列表页
[Deployment列表] 开始恢复状态...
[Deployment列表] sessionStorage 内容: null
[Deployment列表] 无保存状态，执行正常加载

# 点击详情按钮
[Deployment列表] 保存状态到 sessionStorage: {clusterId: 1, namespace: "default", listPath: "/k8s/workloads"}

# 进入详情页
[Deployment详情] 计算 backPath...
[Deployment详情] sessionStorage 内容: {"clusterId":1,"namespace":"default","listPath":"/k8s/workloads"}
[Deployment详情] 返回路径: /k8s/workloads

# 点击返回按钮
[ActionBar] 点击返回按钮，backPath: /k8s/workloads

# 返回列表页
[Deployment列表] 路由变化: {oldPath: "/k8s/resources/deployments/detail", newPath: "/k8s/workloads"}
[Deployment列表] 检测到从详情页返回，触发状态恢复
[Deployment列表] 开始恢复状态...
[Deployment列表] sessionStorage 内容: {"clusterId":1,"namespace":"default","listPath":"/k8s/workloads"}
[Deployment列表] 解析后的状态: {clusterId: 1, namespace: "default", listPath: "/k8s/workloads"}
[Deployment列表] 已设置 clusterId: 1, namespace: default
[Deployment列表] 已清除 sessionStorage
```

---

## 手动验证 sessionStorage

### 在浏览器控制台中执行

```javascript
// 1. 查看当前保存的状态
sessionStorage.getItem('k8s_deployments_list_state')

// 2. 手动设置状态（测试用）
sessionStorage.setItem('k8s_deployments_list_state', JSON.stringify({
  clusterId: 1,
  namespace: 'default',
  listPath: '/k8s/workloads'
}))

// 3. 清除状态
sessionStorage.removeItem('k8s_deployments_list_state')

// 4. 查看所有 sessionStorage 键
Object.keys(sessionStorage)
```

---

## 已添加调试信息的文件

### 列表页
- ✅ `deployments/index.vue`
- ✅ `pods/index.vue`
- ✅ `services/index.vue`
- ✅ `configmaps/index.vue`
- ✅ `secrets/index.vue`

### 详情页
- ✅ `deployments/detail.vue`
- ✅ `pods/detail.vue`
- ✅ `services/detail.vue`
- ✅ `statefulsets/detail.vue`
- ✅ `daemonsets/detail.vue`

### 组件
- ✅ `K8sResourceActionBar.vue`

---

## 下一步

1. **打开浏览器控制台**
2. **按照测试步骤操作**
3. **查看控制台日志**
4. **根据日志内容定位问题**
5. **将日志内容反馈给开发者**
